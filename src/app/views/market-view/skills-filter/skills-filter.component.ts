import { Component, OnInit } from '@angular/core';

// declare const showSliderValue: any;
@Component({
  selector: 'app-skills-filter',
  templateUrl: './skills-filter.component.html',
  styleUrls: ['./skills-filter.component.css']
})
export class SkillsFilterComponent implements OnInit {

  constructor() { }

  ngOnInit(): void {
  }
  
  // Display the default slider value and current value. To be fixed!

  //showSliderValue(){
  //var slider = document.getElementById("myRange") as HTMLInputElement;
  //var output = document.getElementById("demo") as HTMLSpanElement;
  //output.innerHTML = slider.value; // Display the default slider value

// Update the current slider value (each time you drag the slider handle)
  //slider.oninput = function() {
 // output.innerHTML = this.value;
//}

//}
}
